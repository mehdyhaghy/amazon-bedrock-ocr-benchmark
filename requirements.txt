# Core dependencies with pinned versions for security and stability
gradio==6.19.0
boto3==1.43.32

# Image processing dependencies
Pillow==12.2.0
numpy==2.4.6

# Data processing dependencies
pandas==3.0.3
pymupdf==1.27.2.3

# External model providers
cerebras-cloud-sdk==1.67.0  # Native SDK transport for the Cerebras Inference provider (model does the OCR)
